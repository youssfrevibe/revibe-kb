node_modules/
.next/
out/
.env.local
.env*.local
*.tsbuildinfo
next-env.d.ts

# Source material is company IP — kept out of git deliberately.
# Remove this block if you decide the repo is the right home for it.
sources/*
!sources/.gitkeep
!sources/README.md

.vercel
.env*

# Firebase Admin SDK service-account private keys. Downloaded from Firebase
# Console → Project settings → Service accounts. Never commit these — anyone
# with the JSON can mint tokens for the whole project.
revibe-kb-*-firebase-adminsdk-*.json
*-firebase-adminsdk-*.json
vercel_env_setup.md
